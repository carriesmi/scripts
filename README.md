Collection of scripts to use on the go

### Setup aliases
```bash
curl -s "https://raw.githubusercontent.com/carriesmi/scripts/master/alias.sh" >> ~/.bashrc
```
<hr/>

### Setup history related features
```bash

curl -s "https://raw.githubusercontent.com/carriesmi/scripts/master/history.sh" >> ~/.bashrc
```
